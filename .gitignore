# XcodeGen generates these from project.yml — don't commit them
Absumo.xcodeproj/
Absumo/Info.plist

# Build output
build/
DerivedData/
*.xcuserstate
xcuserdata/

# Screenshots from local testing
screenshot-*.png

.DS_Store

# Contains a live Meshly agent token — never commit
.mcp.json

# Piper TTS tooling/model used by scripts/build_audio.sh (large, re-downloadable).
# The generated audio under Absumo/Resources/audio/ IS committed.
scripts/.piper/
